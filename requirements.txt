# Web framework
flask==3.0.3
 
# Reads .env file into os.environ
python-dotenv==1.0.1
 
# Password hashing for auth (never store plain text passwords)
bcrypt==4.1.3
 
# MySQL database connector
mysql-connector-python==8.4.0
 
# ElevenLabs text-to-speech SDK
elevenlabs==1.2.0
 
# HTTP requests (for Pollinations image download)
requests==2.32.3
 
# Image processing — add text/subtitles onto frames
Pillow==10.4.0